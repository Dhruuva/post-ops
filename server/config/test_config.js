 // import user_types from './config'
const user_types = require('./config')
  console.log (user_types, "-----", user_types.permissionLevels.PAID_USER)

  //let l = JSON.stringify(usr);

    console.log (user_types, "-----", JSON.stringify(user_types.permissionLevels))